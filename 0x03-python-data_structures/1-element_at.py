#!/usr/bin/python3

def element_at(my_list, idx):
    while idx < len(my_list):
        if idx < 0:
            return (none)
        elif idx > len(my_list):
            return (none)
        else:
            return (idx + 1)
