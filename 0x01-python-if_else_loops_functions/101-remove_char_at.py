#!/usr/bin/python3
def remove_char_at(str, n):
   # new_str = " "
    for i in range(len(str)):
        if str[i] != str[n]:
           # continue
            i += 1
        return(str)

       # new_str = str[i] - str[n]
       # print('{}' .format(str[i]), end="")
